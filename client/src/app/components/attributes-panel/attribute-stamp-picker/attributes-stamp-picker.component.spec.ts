/* import { ComponentFixture, TestBed, waitForAsync } from '@angular/core/testing';
import { StampPickerViewerService } from '@app/services/tool-modifier/stamp-picker-viewer/stamp-picker-viewer.service';
import { AttributeStampComponent } from './attributes-stamp-picker-viewer.component';
describe('AttributeTextureComponent', () => {
    let component: AttributeStampComponent;
    let fixture: ComponentFixture<AttributeStampComponent>;
    let stampService: StampPickerViewerService;

    beforeEach(
        waitForAsync(() => {
            TestBed.configureTestingModule({
                declarations: [AttributeStampComponent],
                providers: [StampPickerViewerService],
            }).compileComponents();
        }),
    );

    beforeEach(() => {
        fixture = TestBed.createComponent(AttributeStampComponent);
        component = fixture.componentInstance;
        fixture.detectChanges();

        stampService = TestBed.inject(StampPickerViewerService);
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});
 */
